# MNIST Digit Classifier

A web-based MNIST digit classifier built with SvelteKit and TensorFlow.js. Draw digits on a canvas and see real-time predictions from a convolutional neural network.

## Development

```bash
# Install dependencies
npm install

# Start dev server
npm run dev

# Build for production
npm run build

# Deploy to GitHub Pages
npm run deploy
```
